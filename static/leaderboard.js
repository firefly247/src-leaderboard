"use strict";

const API_URL = "https://script.google.com/macros/s/AKfycbwxHRgWZWMisQzVIK8KogLwU2ri5EfhCnY2FqqBLm7Nb8Oo9DYNIF_L28UgMfwO9q2f/exec";
const GITHUB_COMMIT_URL = "https://api.github.com/repos/firefly247/src-leaderboard/commits/main";
const state = { events: [], records: [], competitions: [], competitionEvents: [], competitionDivisions: [], competitionRecords: [], rankings: {}, members: [], competitionMembers: [], clubMedals: {gold:0,silver:0,bronze:0}, pendingDeleteIds: new Set(), adminToken: sessionStorage.getItem("ergAdminToken") || "", refreshing: false, refreshedAt: 0 };
const $ = (s) => document.querySelector(s);
const esc = (v) => String(v ?? "").replace(/[&<>'"]/g, (c) => ({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[c]));
const eventNameCompare = (a,b) => { const an=String(a.event_name||a),bn=String(b.event_name||b),am=an.match(/^\s*(\d+(?:\.\d+)?)/),bm=bn.match(/^\s*(\d+(?:\.\d+)?)/);return am&&bm&&Number(am[1])!==Number(bm[1])?Number(am[1])-Number(bm[1]):an.localeCompare(bn,"ko"); };

function csv(text) { const a=[],r=[];let f="",q=false;for(let i=0;i<text.length;i++){const c=text[i];if(q){if(c==='"'&&text[i+1]==='"'){f+='"';i++;}else if(c==='"')q=false;else f+=c;}else if(c==='"')q=true;else if(c===","){r.push(f);f="";}else if(c==="\n"){r.push(f.replace(/\r$/, ""));if(r.some(Boolean))a.push(r.splice(0));f="";}else f+=c;}r.push(f.replace(/\r$/, ""));if(r.some(Boolean))a.push(r);return a; }
function csvObjects(text) { const rows=csv(text);return rows.length?rows.slice(1).map(r=>Object.fromEntries(rows[0].map((h,i)=>[h.trim(),r[i]||""]))):[]; }
function ms(value) { const t=String(value||"").trim().replace(/\s/g,"").replace(",",".");if(!t)return 0;if(/^\d+(\.\d+)?$/.test(t))return Math.round(Number(t)*1000);const m=t.match(/^(?:(\d+):)?(\d{1,2})(?:\.(\d{1,3}))?$/);return !m||m[1]&&Number(m[2])>=60?0:(Number(m[1]||0)*60+Number(m[2]))*1000+Number((m[3]||"0").padEnd(3,"0").slice(0,3)); }
function time(value) { const s=Math.floor(value/1000),m=Math.floor(s/60),sec=s%60,t=Math.floor(value%1000/100);return `${m}:${String(sec).padStart(2,"0")}.${t}`; }
function record(row) { const timeMs=Number(row.time_ms)||ms(row.time_display);return {...row,memberName:(row.member_name||"").trim(),eventId:row.event_id,eventName:row.event_name,timeMs,timeDisplay:timeMs?time(timeMs):row.time_display}; }
function competitionRecord(row) { return {...row,memberName:(row.member_name||"").trim(),competitionId:row.competition_id,competitionName:row.competition_name,competitionEventId:row.competition_event_id,competitionEventName:row.competition_event_name,competitionDivisionId:row.competition_division_id,competitionDivisionName:row.competition_division_name,year:Number(row.year),gold:Number(row.gold)||0,silver:Number(row.silver)||0,bronze:Number(row.bronze)||0}; }
function medalCount(r) { return r.gold+r.silver+r.bronze; }
function medalText(r) { const parts=[];if(r.gold)parts.push(`금 ${r.gold}`);if(r.silver)parts.push(`은 ${r.silver}`);if(r.bronze)parts.push(`동 ${r.bronze}`);return parts.join(" · ")||"참가"; }
function medalIcons(rows) { if(!rows.length)return '<span class="matrix-empty">-</span>';const icons=[["🥇","gold","금"],["🥈","silver","은"],["🥉","bronze","동"]].filter(([,color])=>rows.some(r=>r[color]>0)).map(([icon,,label])=>`<span class="result-icon" title="${label}메달">${icon}</span>`).join("");return icons||'<span class="result-icon participation" title="참가">👥</span>'; }

function build() {
  const histories=new Map(),best=new Map();
  state.records.forEach(r=>{if(!histories.has(r.memberName))histories.set(r.memberName,[]);histories.get(r.memberName).push(r);const key=r.memberName+"\0"+r.eventId;if(!best.get(key)||r.timeMs<best.get(key).timeMs)best.set(key,r);});
  state.rankings={};state.events.forEach(e=>{const rows=[...best.values()].filter(r=>r.eventId===e.event_id).sort((a,b)=>a.timeMs-b.timeMs||a.memberName.localeCompare(b.memberName,"ko"));let previous,rank;rows.forEach((r,i)=>{r.rank=r.timeMs===previous?rank:i+1;previous=r.timeMs;rank=r.rank;});state.rankings[e.event_id]=rows;});
  const allNames=new Set([...histories.keys(),...state.competitionRecords.map(r=>r.memberName)]);
  state.members=[...allNames].sort((a,b)=>a.localeCompare(b,"ko")).map(memberName=>({memberName,recordCount:(histories.get(memberName)||[]).length,pb:Object.fromEntries(state.events.map(e=>[e.event_id,best.get(memberName+"\0"+e.event_id)||null]))}));
  const competitionMap=new Map(),attendanceKeys=new Set(),clubMedalKeys=new Set();
  state.competitionRecords.forEach(r=>{if(!competitionMap.has(r.memberName))competitionMap.set(r.memberName,{memberName:r.memberName,attendance:0,gold:0,silver:0,bronze:0,medalKeys:new Set()});const m=competitionMap.get(r.memberName),attendanceKey=r.memberName+"\0"+r.year+"\0"+r.competitionId,boatKey=r.year+"\0"+r.competitionId+"\0"+r.competitionEventId+"\0"+r.competitionDivisionId;if(!attendanceKeys.has(attendanceKey)){attendanceKeys.add(attendanceKey);m.attendance++;}["gold","silver","bronze"].forEach(color=>{if(r[color]<=0)return;const memberKey=r.memberName+"\0"+boatKey+"\0"+color;if(!m.medalKeys.has(memberKey)){m.medalKeys.add(memberKey);m[color]++;}clubMedalKeys.add(boatKey+"\0"+color);});});
  state.competitionMembers=[...competitionMap.values()].map(({medalKeys,...member})=>member);
  state.clubMedals={gold:0,silver:0,bronze:0};clubMedalKeys.forEach(key=>{const color=key.split("\0").pop();state.clubMedals[color]++;});
}

function recordTime(r) { return r.proof_photo_url?`<button class="photo-link" type="button" data-photo-url="${esc(r.proof_photo_url)}">${esc(r.timeDisplay)}</button>`:esc(r.timeDisplay); }
function deleteControl(r) { return state.pendingDeleteIds.has(r.record_id)?'<button class="pending-delete-button" type="button" disabled>pending</button>':`<button class="delete-request-button" data-record-id="${esc(r.record_id)}">삭제</button>`; }
function eventSection(e) { const rows=state.rankings[e.event_id]||[],podium=[2,1,3].map(n=>{const r=rows[n-1],name=r?r.memberName:"기록 없음",competition=r?r.competition:"기록을 기다리고 있습니다.";return `<article class="podium-card podium-${n}"><div class="medal">${n}</div><h3 title="${esc(name)}">${esc(name)}</h3><div class="record-time">${r?recordTime(r):"-"}</div><p>${esc(competition)}</p><small>${esc(r?r.competition_date:"")}</small></article>`;}).join("");const body=rows.map(r=>`<tr><td><span class="rank-badge">${r.rank}</span></td><td class="member-name">${esc(r.memberName)}</td><td class="time-cell">${recordTime(r)}</td></tr>`).join("")||'<tr><td colspan="3" class="empty-cell">등록된 기록이 없습니다.</td></tr>';return `<section class="event-section"><h2 class="event-title">${esc(e.event_name)}</h2><section class="podium-section"><div class="section-heading event-heading"><p class="eyebrow">TOP 3</p></div><div class="podium-grid">${podium}</div></section><section class="panel"><div class="section-heading table-heading"><p class="eyebrow">RANKING</p><input class="search-input event-search" type="search" placeholder="${esc(e.event_name)} 이름 검색" data-ranking-table="ranking-${esc(e.event_id)}"></div><div class="table-wrap ranking-scroll"><table id="ranking-${esc(e.event_id)}" class="ranking-table"><thead><tr><th>순위</th><th>이름</th><th>PB</th></tr></thead><tbody>${body}</tbody></table></div></section></section>`; }
function renderErgo() {
  $("#topChampions").innerHTML=state.events.map(e=>{const r=(state.rankings[e.event_id]||[])[0];return `<article class="top-champion-card"><span>${esc(e.event_name)} TOP 1</span><strong>${esc(r?r.memberName:"-")}</strong><small>${esc(r?r.timeDisplay:"-")}</small></article>`;}).join("");
  $("#eventSections").innerHTML=state.events.map(eventSection).join("");
  $("#memberTable thead").innerHTML=`<tr><th>#</th><th>이름</th>${state.events.map(e=>`<th>${esc(e.event_name)}</th>`).join("")}<th>등록 기록</th><th>상세</th></tr>`;
  $("#memberTable tbody").innerHTML=state.members.map((m,i)=>`<tr><td>${i+1}</td><td class="member-name">${esc(m.memberName)}</td>${state.events.map(e=>`<td>${esc(m.pb[e.event_id]?m.pb[e.event_id].timeDisplay:"-")}</td>`).join("")}<td>${m.recordCount}</td><td><button class="record-detail-button" data-member-index="${i}">기록</button></td></tr>`).join("")||`<tr><td colspan="${state.events.length+4}" class="empty-cell">등록된 회원이 없습니다.</td></tr>`;
}
function topFiveRows(rows,type) { return rows.slice(0,5).map((m,i)=>`<article class="top-five-row"><span class="top-five-rank">${i+1}</span><strong>${esc(m.memberName)}</strong>${type==="attendance"?`<span><b>${m.attendance}</b>회 출전</span>`:`<span class="medal-score"><i class="gold">${m.gold}</i><i class="silver">${m.silver}</i><i class="bronze">${m.bronze}</i><small>총 ${m.gold+m.silver+m.bronze}</small></span>`}</article>`).join("")||'<p class="empty-cell">등록된 대회 기록이 없습니다.</p>'; }
function renderCompetition() {
  const total=state.clubMedals;
  $("#clubMedalTotal").innerHTML=`<div class="total-medals"><span>총 메달</span><strong>${total.gold+total.silver+total.bronze}</strong></div><div class="medal-total gold"><span>금</span><strong>${total.gold}</strong></div><div class="medal-total silver"><span>은</span><strong>${total.silver}</strong></div><div class="medal-total bronze"><span>동</span><strong>${total.bronze}</strong></div>`;
  const attendance=[...state.competitionMembers].sort((a,b)=>b.attendance-a.attendance||b.gold-a.gold||b.silver-a.silver||b.bronze-a.bronze||a.memberName.localeCompare(b.memberName,"ko"));
  const medals=[...state.competitionMembers].filter(m=>m.gold+m.silver+m.bronze>0).sort((a,b)=>b.gold-a.gold||b.silver-a.silver||b.bronze-a.bronze||b.attendance-a.attendance||a.memberName.localeCompare(b.memberName,"ko"));
  $("#attendanceTop5").innerHTML=topFiveRows(attendance,"attendance");$("#medalTop5").innerHTML=topFiveRows(medals,"medals");
  const years=[...new Set(state.competitionRecords.map(r=>r.year))].sort((a,b)=>b-a),current=$("#competitionYearFilter").value;$("#competitionYearFilter").innerHTML='<option value="">전체 연도</option>'+years.map(y=>`<option value="${y}" ${String(y)===current?"selected":""}>${y}년</option>`).join("");renderCompetitionTables();
}
function renderCompetitionTables() {
  const year=Number($("#competitionYearFilter").value)||0,rows=state.competitionRecords.filter(r=>!year||r.year===year),summary=new Map();
  $("#competitionSummaryHead").innerHTML=`<tr><th>연도</th><th>대회</th><th>종목</th>${state.competitionDivisions.map(d=>`<th>${esc(d.competition_division_name).replace(" ","<br>")}</th>`).join("")}</tr>`;
  rows.forEach(r=>{const key=r.year+"\0"+r.competitionId+"\0"+r.competitionEventId;if(!summary.has(key))summary.set(key,{year:r.year,name:r.competitionName,eventName:r.competitionEventName,divisions:new Map()});const divisions=summary.get(key).divisions;if(!divisions.has(r.competitionDivisionId))divisions.set(r.competitionDivisionId,[]);divisions.get(r.competitionDivisionId).push(r);});
  const summaryRows=[...summary.values()].sort((a,b)=>b.year-a.year||a.name.localeCompare(b.name,"ko")||a.eventName.localeCompare(b.eventName,"ko"));
  const yearRowCounts=summaryRows.reduce((counts,row)=>(counts.set(row.year,(counts.get(row.year)||0)+1),counts),new Map());
  let renderedYear;
  $("#competitionSummaryBody").innerHTML=summaryRows.map(s=>{const yearCell=s.year!==renderedYear?`<td class="matrix-year" rowspan="${yearRowCounts.get(s.year)}">${s.year}</td>`:"";renderedYear=s.year;return `<tr>${yearCell}<td class="member-name matrix-competition" title="${esc(s.name)}">${esc(s.name)}</td><td class="matrix-event"><strong>${esc(s.eventName)}</strong></td>${state.competitionDivisions.map(d=>`<td class="matrix-result">${medalIcons(s.divisions.get(d.competition_division_id)||[])}</td>`).join("")}</tr>`;}).join("")||`<tr><td colspan="${state.competitionDivisions.length+3}" class="empty-cell">해당 연도의 대회 기록이 없습니다.</td></tr>`;
  $("#competitionHistoryBody").innerHTML=state.competitionMembers.slice().sort((a,b)=>a.memberName.localeCompare(b.memberName,"ko")).map((m,i)=>`<tr><td>${i+1}</td><td class="member-name">${esc(m.memberName)}</td><td>${m.attendance}</td><td class="medal-number gold">${m.gold}</td><td class="medal-number silver">${m.silver}</td><td class="medal-number bronze">${m.bronze}</td><td><strong>${m.gold+m.silver+m.bronze}</strong></td><td><button class="record-detail-button" data-competition-member="${esc(m.memberName)}">기록</button></td></tr>`).join("")||'<tr><td colspan="8" class="empty-cell">등록된 회원 대회 기록이 없습니다.</td></tr>';
}
function renderForms() {
  const members=state.members.map(m=>`<option value="${esc(m.memberName)}">${esc(m.memberName)}</option>`).join("");
  $("#memberSelect").innerHTML='<option value="" selected disabled>회원 선택</option><option value="__new__">회원명 직접 입력</option>'+members;$("#competitionMemberSelect").innerHTML='<option value="" selected disabled>회원 선택</option><option value="__new__">회원명 직접 입력</option>'+members;
  $("#requestEvent").innerHTML='<option value="" selected disabled>종목 선택</option><option value="__new__">종목명 직접 입력</option>'+state.events.map(e=>`<option value="${esc(e.event_id)}">${esc(e.event_name)}</option>`).join("");
  const places=[...new Set(state.records.map(r=>(r.competition||"").trim()).filter(Boolean))].sort((a,b)=>a.localeCompare(b,"ko"));$("#competitionSelect").innerHTML='<option value="" selected disabled>대회/장소 선택</option><option value="__new__">대회/장소명 직접 입력</option>'+places.map(x=>`<option value="${esc(x)}">${esc(x)}</option>`).join("");
  $("#raceSelect").innerHTML='<option value="" selected disabled>대회 선택</option><option value="__new__">새 대회 등록</option>'+state.competitions.slice().sort((a,b)=>a.competition_name.localeCompare(b.competition_name,"ko")).map(c=>`<option value="${esc(c.competition_id)}">${esc(c.competition_name)}</option>`).join("");
  $("#competitionEventSelect").innerHTML='<option value="" selected disabled>종목 선택</option><option value="__new__">새 종목 등록</option>'+state.competitionEvents.map(e=>`<option value="${esc(e.competition_event_id)}">${esc(e.competition_event_name)}</option>`).join("");
  $("#competitionDivisionSelect").innerHTML='<option value="" selected disabled>나이대 선택</option><option value="__new__">새 나이대 등록</option>'+state.competitionDivisions.map(d=>`<option value="${esc(d.competition_division_id)}">${esc(d.competition_division_name)}</option>`).join("");
  const currentYear=new Date().getFullYear();$("#competitionYearSelect").innerHTML=Array.from({length:currentYear-1996},(_,i)=>currentYear+3-i).map(y=>`<option value="${y}" ${y===currentYear?"selected":""}>${y}년</option>`).join("");
  ["#goldMedalSelect","#silverMedalSelect","#bronzeMedalSelect"].forEach(selector=>{$(selector).innerHTML=Array.from({length:100},(_,i)=>`<option value="${i}" ${i===0?"selected":""}>${i}개</option>`).join("");});
  syncWheelTriggers();
}
function render() { renderErgo();renderCompetition();renderForms(); }
function openMember(i) { const m=state.members[i];if(!m)return;$("#dialogMemberName").textContent=m.memberName+" 선수 기록";$("#dialogContent").innerHTML=`<div class="member-pb-grid">${state.events.map(e=>{const r=m.pb[e.event_id];return `<div class="member-pb-card"><span>${esc(e.event_name)} PB</span><strong>${esc(r?r.timeDisplay:"-")}</strong><small>${esc(r?r.competition_date:"")}</small></div>`;}).join("")}</div><div class="member-history-list">${state.events.map(e=>{const rows=state.records.filter(r=>r.memberName===m.memberName&&r.eventId===e.event_id).sort((a,b)=>(b.competition_date||"").localeCompare(a.competition_date||"")||a.timeMs-b.timeMs);return `<section class="member-event-history"><div class="member-event-heading"><h3>${esc(e.event_name)}</h3><span>${rows.length}개 기록</span></div><div class="table-wrap"><table class="member-history-table"><thead><tr><th>날짜</th><th>기록</th><th>대회/장소</th><th>삭제</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${esc(r.competition_date||"-")}</td><td class="time-cell">${recordTime(r)}</td><td>${esc(r.competition||"-")}</td><td>${deleteControl(r)}</td></tr>`).join("")||'<tr><td colspan="4" class="empty-cell">기록이 없습니다.</td></tr>'}</tbody></table></div></section>`;}).join("")}</div>`;$("#memberDialog").showModal(); }
function openCompetitionMember(memberName) { const member=state.competitionMembers.find(m=>m.memberName===memberName),rows=state.competitionRecords.filter(r=>r.memberName===memberName).sort((a,b)=>b.year-a.year||a.competitionName.localeCompare(b.competitionName,"ko"));if(!member)return;$("#dialogMemberName").textContent=memberName+" 대회 기록";$("#dialogContent").innerHTML=`<div class="member-pb-grid competition-medal-cards"><div class="member-pb-card"><span>출전</span><strong>${member.attendance}</strong><small>대회</small></div><div class="member-pb-card"><span>🥇 금메달</span><strong>${member.gold}</strong><small>개</small></div><div class="member-pb-card"><span>🥈 은메달</span><strong>${member.silver}</strong><small>개</small></div><div class="member-pb-card"><span>🥉 동메달</span><strong>${member.bronze}</strong><small>개</small></div></div><div class="member-history-list"><div class="table-wrap"><table class="competition-detail-table"><thead><tr><th>연도</th><th>대회</th><th>종목</th><th>나이대</th><th>결과</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${r.year}</td><td>${esc(r.competitionName)}</td><td>${esc(r.competitionEventName)}</td><td>${esc(r.competitionDivisionName)}</td><td>${medalIcons([r])}</td></tr>`).join("")}</tbody></table></div></div>`;$("#memberDialog").showModal(); }

async function api(action,payload={},admin=false) { const res=await fetch(API_URL,{method:"POST",headers:{"Content-Type":"text/plain;charset=utf-8"},body:JSON.stringify({action,payload,adminToken:admin?state.adminToken:""})}),data=await res.json();if(!res.ok||!data.ok)throw new Error(data.message||"요청에 실패했습니다.");return data; }
async function compress(file) { if(!file)return null;if(file.size>10*1024*1024)throw new Error("증빙사진은 원본 파일 기준 10MB 이하여야 합니다.");const image=await createImageBitmap(file);let scale=Math.min(1,1600/Math.max(image.width,image.height)),quality=.82,blob;do{const c=document.createElement("canvas");c.width=Math.round(image.width*scale);c.height=Math.round(image.height*scale);c.getContext("2d").drawImage(image,0,0,c.width,c.height);blob=await new Promise(r=>c.toBlob(r,"image/jpeg",quality));scale*=.8;quality=Math.max(.45,quality-.08);}while(blob&&blob.size>307200&&scale>.1);if(!blob||blob.size>307200)throw new Error("사진을 300KB 이하로 압축하지 못했습니다.");const base64=await new Promise((ok,no)=>{const r=new FileReader();r.onload=()=>ok(String(r.result).split(",")[1]);r.onerror=no;r.readAsDataURL(blob);});return {name:file.name.replace(/\.[^.]+$/,"")+".jpg",mimeType:"image/jpeg",base64}; }
async function fetchWithTimeout(url) { const controller=new AbortController(),timeout=window.setTimeout(()=>controller.abort(),12000);try{return await fetch(url,{cache:"no-store",signal:controller.signal});}finally{window.clearTimeout(timeout);} }
async function latestRevision() { try{const r=await fetchWithTimeout(`${GITHUB_COMMIT_URL}?_=${Date.now()}`),d=await r.json();return /^[a-f0-9]{40}$/i.test(d.sha||"")?d.sha:"main";}catch{return "main";} }
async function fetchCsv(name,revision) { const local=["localhost","127.0.0.1"].includes(location.hostname),url=local?`data/${name}?_=${Date.now()}`:`https://raw.githubusercontent.com/firefly247/src-leaderboard/${revision}/data/${name}?_=${Date.now()}`,r=await fetchWithTimeout(url);if(!r.ok)throw new Error(`${name} 파일을 불러오지 못했습니다.`);return r.text(); }
async function refresh(manual=false) { if(state.refreshing)return;const b=$("#refreshButton"),s=$("#statusMessage");state.refreshing=true;b.disabled=true;b.setAttribute("aria-busy","true");if(manual){s.hidden=false;s.classList.remove("status-error");s.textContent="최신 기록을 확인하는 중입니다.";}try{const revision=await latestRevision(),files=await Promise.all(["events.csv","records.csv","competitions.csv","competition_events.csv","competition_divisions.csv","competition_records.csv"].map(n=>fetchCsv(n,revision)));state.events=csvObjects(files[0]).filter(e=>e.event_id&&e.event_name).sort(eventNameCompare);state.records=csvObjects(files[1]).map(record).filter(r=>r.memberName&&r.eventId&&r.timeMs>0);state.competitions=csvObjects(files[2]).filter(c=>c.competition_id&&c.competition_name);state.competitionEvents=csvObjects(files[3]).filter(e=>e.competition_event_id&&e.competition_event_name);state.competitionDivisions=csvObjects(files[4]).filter(d=>d.competition_division_id&&d.competition_division_name);state.competitionRecords=csvObjects(files[5]).map(competitionRecord).filter(r=>r.memberName&&r.competitionId&&r.competitionEventId&&r.competitionDivisionId&&r.year);build();render();if(manual)s.hidden=true;api("pendingDeleteRecords").then(d=>state.pendingDeleteIds=new Set(d.recordIds||[])).catch(()=>{});}catch(e){s.hidden=false;s.classList.add("status-error");s.textContent=e.message;console.error(e);}finally{state.refreshing=false;state.refreshedAt=Date.now();b.disabled=false;b.removeAttribute("aria-busy");} }
function result(selector,message,bad=false) { const e=$(selector);e.textContent=message;e.classList.toggle("is-error",bad); }

function adminContent(view,data) {
  const c=$("#adminContent");
  if(["events","competitions","competition-events","competition-divisions"].includes(view)){const configs={events:{items:data.events,idKey:"event_id",nameKey:"event_name",noun:"에르고 종목",kind:"event"},competitions:{items:data.competitions,idKey:"competition_id",nameKey:"competition_name",noun:"대회",kind:"competition"},"competition-events":{items:data.competitionEvents,idKey:"competition_event_id",nameKey:"competition_event_name",noun:"대회 종목",kind:"competition-event"},"competition-divisions":{items:data.competitionDivisions,idKey:"competition_division_id",nameKey:"competition_division_name",noun:"나이대",kind:"competition-division"}},config=configs[view],items=config.items||[];c.innerHTML=`<p class="eyebrow">ADMIN</p><h2>${config.noun} 목록 관리</h2><form id="${config.kind}AddForm" class="inline-form"><input name="itemName" maxlength="100" placeholder="새 ${config.noun} 이름" required><button class="primary-button">${config.noun} 추가</button></form><div class="table-wrap"><table class="admin-table"><thead><tr><th>${config.noun}명</th><th>관리</th></tr></thead><tbody>${items.map(x=>`<tr><td><input value="${esc(x[config.nameKey])}" data-${config.kind}-name="${esc(x[config.idKey])}"></td><td><button class="secondary-button item-save" data-kind="${config.kind}" data-item-id="${esc(x[config.idKey])}">이름 저장</button> <button class="delete-request-button item-delete" data-kind="${config.kind}" data-item-id="${esc(x[config.idKey])}">삭제</button></td></tr>`).join("")||'<tr><td colspan="2" class="empty-cell">등록된 항목이 없습니다.</td></tr>'}</tbody></table></div>`;return;}
  const titles={"competition-add":"대회 기록 요청",add:"에르고 기록 요청",delete:"기록 삭제 요청",history:"처리 이력"},rows=(data.requests||[]).map(r=>`<tr><td>${esc(r.requested_at)}</td><td>${esc(r.member_name)}</td><td>${esc(r.event_name||"-")}</td><td>${esc(r.competition_date||"-")}</td><td>${esc(r.time_display||"-")}</td><td>${esc(r.status)}</td><td>${r.proof_photo_url?`<button class="photo-link" data-photo-url="${esc(r.proof_photo_url)}">사진</button>`:"-"}</td><td>${r.status==="pending"?`<button class="primary-button admin-action" data-id="${esc(r.request_id)}" data-decision="approved">승인</button> <button class="secondary-button admin-action" data-id="${esc(r.request_id)}" data-decision="rejected">거절</button>`:"-"}</td></tr>`).join("")||'<tr><td colspan="8" class="empty-cell">표시할 요청이 없습니다.</td></tr>';c.innerHTML=`<p class="eyebrow">ADMIN</p><h2>${titles[view]}</h2><div class="table-wrap"><table class="admin-table"><thead><tr><th>요청 시각</th><th>이름</th><th>종목/대회</th><th>날짜/연도</th><th>기록/메달</th><th>상태</th><th>사진</th><th>처리</th></tr></thead><tbody>${rows}</tbody></table></div>`;
}
async function loadAdmin(view) { document.querySelectorAll("[data-admin-view]").forEach(b=>b.classList.toggle("is-active",b.dataset.adminView===view));$("#adminContent").innerHTML="<p>불러오는 중입니다.</p>";try{adminContent(view,await api("adminList",{view},true));}catch(e){$("#adminContent").innerHTML=`<p class="form-result is-error">${esc(e.message)}</p>`;} }
function bindSelect(select,input,lookup) { $(select).onchange=e=>{const direct=e.target.value==="__new__",found=lookup?lookup(e.target.value):null;$(input).value=direct?"":found||e.target.value;$(input).readOnly=!direct;if(direct)$(input).focus();}; }
let wheelTarget=null,wheelPending="",wheelScrollTimer=0;
function syncWheelTrigger(select) { if(!select?._wheelTrigger)return;const option=select.options[select.selectedIndex];select._wheelTrigger.textContent=option&&!option.disabled?option.textContent:(option?.textContent||"선택");select._wheelTrigger.classList.toggle("is-placeholder",!select.value); }
function syncWheelTriggers() { document.querySelectorAll(".wheel-source").forEach(syncWheelTrigger); }
function selectNearestWheelOption() { if(!wheelTarget)return;const list=$("#wheelPickerList"),center=list.getBoundingClientRect().top+list.clientHeight/2,options=[...list.querySelectorAll(".wheel-option")];if(!options.length)return;const nearest=options.reduce((best,item)=>Math.abs(item.getBoundingClientRect().top+item.offsetHeight/2-center)<Math.abs(best.getBoundingClientRect().top+best.offsetHeight/2-center)?item:best);wheelPending=nearest.dataset.value;options.forEach(item=>item.classList.toggle("is-selected",item===nearest)); }
function openWheelPicker(select) { const options=[...select.options].filter(option=>!option.disabled);if(!options.length)return;const fallback=options.find(option=>option.value!=="__new__")||options[0];wheelTarget=select;wheelPending=select.value&&options.some(option=>option.value===select.value)?select.value:fallback.value;$("#wheelPickerTitle").textContent=select.getAttribute("aria-label")||select.closest("label")?.querySelector("span")?.textContent.trim()||"옵션 선택";$("#wheelPickerList").innerHTML=options.map(option=>`<button class="wheel-option ${option.value===wheelPending?"is-selected":""}" type="button" data-value="${esc(option.value)}">${esc(option.textContent)}</button>`).join("");$("#wheelPickerDialog").showModal();requestAnimationFrame(()=>$("#wheelPickerList .is-selected")?.scrollIntoView({block:"center"})); }
function setupWheelPickers() { document.querySelectorAll(".wheel-source").forEach(select=>{select.classList.add("wheel-native");const button=document.createElement("button");button.type="button";button.className="wheel-trigger";button.setAttribute("aria-haspopup","dialog");button.onclick=()=>openWheelPicker(select);select.after(button);select._wheelTrigger=button;select.addEventListener("change",()=>syncWheelTrigger(select));syncWheelTrigger(select);});const list=$("#wheelPickerList");list.addEventListener("scroll",()=>{clearTimeout(wheelScrollTimer);wheelScrollTimer=setTimeout(selectNearestWheelOption,70);});list.onclick=e=>{const option=e.target.closest(".wheel-option");if(!option)return;wheelPending=option.dataset.value;option.scrollIntoView({behavior:"smooth",block:"center"});};$("#wheelPickerCancel").onclick=()=>$("#wheelPickerDialog").close();$("#wheelPickerConfirm").onclick=()=>{selectNearestWheelOption();if(wheelTarget){wheelTarget.value=wheelPending;wheelTarget.dispatchEvent(new Event("change",{bubbles:true}));}$("#wheelPickerDialog").close();};$("#wheelPickerDialog").onclick=e=>{if(e.target===e.currentTarget)e.currentTarget.close();}; }

$("#currentYear").textContent=new Date().getFullYear();
document.querySelectorAll(".tab-button").forEach(button=>button.onclick=()=>{document.querySelectorAll(".tab-button").forEach(x=>x.classList.toggle("is-active",x===button));document.querySelectorAll(".tab-panel").forEach(x=>x.classList.toggle("is-active",x.dataset.panel===button.dataset.tab));history.replaceState(null,"","#"+button.dataset.tab);if(button.dataset.tab==="admin"&&state.adminToken)loadAdmin("add");});
document.querySelectorAll("[data-request-view]").forEach(button=>button.onclick=()=>{document.querySelectorAll("[data-request-view]").forEach(x=>x.classList.toggle("is-active",x===button));document.querySelectorAll("[data-request-panel]").forEach(x=>x.classList.toggle("is-active",x.dataset.requestPanel===button.dataset.requestView));});
$("#refreshButton").onclick=()=>refresh(true);$("#competitionYearFilter").onchange=renderCompetitionTables;
$("#memberSearch").oninput=e=>document.querySelectorAll("#memberTable tbody tr").forEach(r=>r.hidden=!r.textContent.toLocaleLowerCase("ko").includes(e.target.value.trim().toLocaleLowerCase("ko")));
$("#competitionMemberSearch").oninput=e=>document.querySelectorAll("#competitionHistoryBody tr").forEach(r=>r.hidden=!r.textContent.toLocaleLowerCase("ko").includes(e.target.value.trim().toLocaleLowerCase("ko")));
$("#eventSections").oninput=e=>{if(e.target.dataset.rankingTable)document.querySelectorAll("#"+e.target.dataset.rankingTable+" tbody tr").forEach(r=>r.hidden=!r.textContent.toLocaleLowerCase("ko").includes(e.target.value.trim().toLocaleLowerCase("ko")));};
$("#memberTable").onclick=e=>{const b=e.target.closest("[data-member-index]");if(b)openMember(Number(b.dataset.memberIndex));};
$("#competitionMemberTable").onclick=e=>{const b=e.target.closest("[data-competition-member]");if(b)openCompetitionMember(b.dataset.competitionMember);};
bindSelect("#memberSelect","#memberNameInput");bindSelect("#competitionMemberSelect","#competitionMemberInput");bindSelect("#competitionSelect","#competitionInput");bindSelect("#requestEvent","#eventNameInput",id=>state.events.find(x=>x.event_id===id)?.event_name);bindSelect("#raceSelect","#raceNameInput",id=>state.competitions.find(x=>x.competition_id===id)?.competition_name);bindSelect("#competitionEventSelect","#competitionEventNameInput",id=>state.competitionEvents.find(x=>x.competition_event_id===id)?.competition_event_name);bindSelect("#competitionDivisionSelect","#competitionDivisionNameInput",id=>state.competitionDivisions.find(x=>x.competition_division_id===id)?.competition_division_name);
$("#dialogCloseButton").onclick=()=>$("#memberDialog").close();$("#memberDialog").onclick=e=>{if(e.target===e.currentTarget)e.currentTarget.close();};

document.addEventListener("click",async e=>{
  const photo=e.target.closest("[data-photo-url]");if(photo){$("#proofPhotoImage").src=photo.dataset.photoUrl;$("#photoDialog").showModal();return;}if(e.target.closest("[data-close-photo]"))$("#photoDialog").close();
  const del=e.target.closest("[data-record-id]");if(del){const r=state.records.find(x=>x.record_id===del.dataset.recordId),reason=prompt("삭제 사유를 입력하세요.");if(reason?.trim())try{await api("requestDelete",{recordId:r.record_id,reason:reason.trim()});state.pendingDeleteIds.add(r.record_id);del.disabled=true;del.textContent="pending";}catch(x){alert(x.message);}}
  const action=e.target.closest(".admin-action");if(action)try{await api("processRequest",{requestId:action.dataset.id,decision:action.dataset.decision},true);await loadAdmin("history");await refresh();}catch(x){alert(x.message);}
  const save=e.target.closest(".item-save");if(save){const configs={event:{action:"manageEvent",id:"eventId",name:"eventName",view:"events"},competition:{action:"manageCompetition",id:"competitionId",name:"competitionName",view:"competitions"},"competition-event":{action:"manageCompetitionEvent",id:"competitionEventId",name:"competitionEventName",view:"competition-events"},"competition-division":{action:"manageCompetitionDivision",id:"competitionDivisionId",name:"competitionDivisionName",view:"competition-divisions"}},config=configs[save.dataset.kind],selector=`[data-${save.dataset.kind}-name="${save.dataset.itemId}"]`;try{await api(config.action,{operation:"rename",[config.id]:save.dataset.itemId,[config.name]:$(selector).value},true);await refresh();await loadAdmin(config.view);}catch(x){alert(x.message);}}
  const remove=e.target.closest(".item-delete");if(remove&&confirm("이 항목을 삭제할까요?")){const configs={event:{action:"manageEvent",id:"eventId",view:"events"},competition:{action:"manageCompetition",id:"competitionId",view:"competitions"},"competition-event":{action:"manageCompetitionEvent",id:"competitionEventId",view:"competition-events"},"competition-division":{action:"manageCompetitionDivision",id:"competitionDivisionId",view:"competition-divisions"}},config=configs[remove.dataset.kind];try{await api(config.action,{operation:"delete",[config.id]:remove.dataset.itemId},true);await refresh();await loadAdmin(config.view);}catch(x){alert(x.message);}}
});

$("#recordRequestForm").onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,f=new FormData(form),memberName=String(f.get("memberName")).trim(),eventName=String(f.get("eventName")).trim(),minutes=Number(f.get("minutes")),seconds=Number(f.get("seconds")),tenths=Number(f.get("tenths"));if(!memberName||!eventName)return result("#recordRequestResult","이름과 종목명을 입력해 주세요.",true);if(![minutes,seconds,tenths].every(Number.isInteger)||seconds<0||seconds>59||tenths<0||tenths>9)return result("#recordRequestResult","기록 값을 확인해 주세요.",true);try{result("#recordRequestResult","사진을 처리하고 요청을 전송하는 중입니다.");await api("requestAdd",{memberName,competitionDate:f.get("competitionDate"),eventId:f.get("eventId")==="__new__"?"":f.get("eventId"),eventName,timeDisplay:`${minutes}:${String(seconds).padStart(2,"0")}.${tenths}`,competition:String(f.get("competition")).trim(),note:String(f.get("note")).trim(),photo:await compress(f.get("proofPhoto"))});form.reset();["#memberNameInput","#eventNameInput","#competitionInput"].forEach(x=>$(x).readOnly=true);result("#recordRequestResult","에르고 기록 요청이 접수되었습니다.");}catch(x){result("#recordRequestResult",x.message,true);}};
$("#competitionRequestForm").onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,f=new FormData(form),payload={memberName:String(f.get("memberName")).trim(),competitionId:f.get("competitionId")==="__new__"?"":f.get("competitionId"),competitionName:String(f.get("competitionName")).trim(),competitionEventId:f.get("competitionEventId")==="__new__"?"":f.get("competitionEventId"),competitionEventName:String(f.get("competitionEventName")).trim(),competitionDivisionId:f.get("competitionDivisionId")==="__new__"?"":f.get("competitionDivisionId"),competitionDivisionName:String(f.get("competitionDivisionName")).trim(),year:Number(f.get("year")),gold:Number(f.get("gold")),silver:Number(f.get("silver")),bronze:Number(f.get("bronze")),note:String(f.get("note")).trim()};if(!payload.memberName||!payload.competitionName||!payload.competitionEventName||!payload.competitionDivisionName)return result("#competitionRequestResult","이름, 대회명, 종목, 나이대를 입력해 주세요.",true);if(!Number.isInteger(payload.year)||payload.year<1900||payload.year>2100||![payload.gold,payload.silver,payload.bronze].every(v=>Number.isInteger(v)&&v>=0))return result("#competitionRequestResult","연도와 메달 수를 확인해 주세요.",true);try{result("#competitionRequestResult","요청을 전송하는 중입니다.");await api("requestCompetitionAdd",payload);form.reset();["#competitionMemberInput","#raceNameInput","#competitionEventNameInput","#competitionDivisionNameInput"].forEach(x=>$(x).readOnly=true);syncWheelTriggers();result("#competitionRequestResult","대회 기록 요청이 접수되었습니다.");}catch(x){result("#competitionRequestResult",x.message,true);}};
$("#adminLoginForm").onsubmit=async e=>{e.preventDefault();try{const d=await api("adminLogin",{password:new FormData(e.currentTarget).get("password")});state.adminToken=d.adminToken;sessionStorage.setItem("ergAdminToken",d.adminToken);$("#adminLogin").hidden=true;$("#adminConsole").hidden=false;loadAdmin("add");}catch(x){result("#adminLoginResult",x.message,true);}};
$("#adminConsole").onclick=e=>{const b=e.target.closest("[data-admin-view]");if(b)loadAdmin(b.dataset.adminView);};$("#adminLogout").onclick=()=>{state.adminToken="";sessionStorage.removeItem("ergAdminToken");$("#adminLogin").hidden=false;$("#adminConsole").hidden=true;};
$("#adminContent").onsubmit=async e=>{const configs={eventAddForm:{action:"manageEvent",name:"eventName",view:"events"},competitionAddForm:{action:"manageCompetition",name:"competitionName",view:"competitions"},"competition-eventAddForm":{action:"manageCompetitionEvent",name:"competitionEventName",view:"competition-events"},"competition-divisionAddForm":{action:"manageCompetitionDivision",name:"competitionDivisionName",view:"competition-divisions"}},config=configs[e.target.id];if(!config)return;e.preventDefault();const name=new FormData(e.target).get("itemName");try{await api(config.action,{operation:"add",[config.name]:name},true);await refresh();loadAdmin(config.view);}catch(x){alert(x.message);}};

setupWheelPickers();if(state.adminToken){$("#adminLogin").hidden=true;$("#adminConsole").hidden=false;}const initialTab=location.hash.slice(1);if(["competition","request","admin"].includes(initialTab))document.querySelector(`[data-tab="${initialTab}"]`).click();const refreshWhenVisible=()=>{if(!document.hidden&&Date.now()-state.refreshedAt>60000)refresh();};window.addEventListener("pageshow",refreshWhenVisible);window.addEventListener("focus",refreshWhenVisible);document.addEventListener("visibilitychange",refreshWhenVisible);setInterval(()=>{if(!document.hidden)refresh();},300000);refresh();
